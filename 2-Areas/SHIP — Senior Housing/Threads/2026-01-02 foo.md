---
subject: "foo"
message_count: 1
first: 2026-01-02
last: 2026-01-02
senders:
  - jason@ynkr.org
type: email-thread
tags: [email]
---

# foo

**1 message** on 2026-01-02

## 1. 2026-01-02 19:55 — jason@ynkr.org

# http://book.git-scm.com/5_customizing_git.html
# http://www.jukie.net/bart/blog/pimping-out-git-log

[user]
        name = Jason Younker
        email = jason@ynkr.org
[color]
  diff = auto
  status = auto
  branch = auto
  ui = auto
[apply]
  whitespace = nowarn
[push]
  default = tracking
[github]
  user = younker
  token = 82c31624dc1fc85c84d3725a751fb4c
[rerere]
  enabled = 1
[branch]
  # http://d.strelau.net/post/47338904/git-pull-rebase-by-default
  autosetuprebase = always
[core]
  editor = emacs
[alias]
  co = checkout
  st = status
  br = branch --sort=-committerdate
  unstage = reset HEAD
  # last = cat-file commit HEAD
  last = log -1 HEAD
  recent = log -4 HEAD
  logs = log --stat
  logg = log --graph --all --decorate --stat --date=iso
  # http://git-scm.com/book/en/Customizing-Git-Git-Configuration
  # Colors: normal, black, red, green, yellow, blue, magenta, cyan, or white
  # Format: bold, dim, ul, blink, and reverse
  # ynkr: removed '-n 15' before '--graph'
  slog = "log --graph --pretty=format:'%C(red bold)%h%Creset %C(green)%ad %C(red)%an - %C(yellow)%d%Creset %s %Creset' --abbrev-commit --date=relative"

  # https://csswizardry.com/2017/05/little-things-i-like-to-do-with-git/
  leaderboard = shortlog -sn
  recently = shortlog -sn --since='1 week'
  recap = log --all --oneline --no-merges --author=jason@ynkr.org

  # Temporarily ignore files with changes
  freeze = update-index --assume-unchanged
  frozen = !git ls-files -v | grep "^[[:lower:]]"
  thaw = update-index --no-assume-unchanged

  has = branch -a --contains
  # restore = "!f() { git checkout $(git rev-list -n 1 HEAD -- $1)~1 -- $(git diff --name-status $(git rev-list -n 1 HEAD -- $1)~1 | grep '^D' | cut -f 2); }; f"

---
